<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>folder X ToychReaLlife</title>
<style>
body {
  margin: 0;
  overflow: hidden;
  background: radial-gradient(circle at center, #000, #110000);
  font-family: 'Orbitron', sans-serif;
  color: #fff;
}

#loading {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  background: linear-gradient(135deg, #000, #220000);
  z-index: 10000;
  animation: fadeOut 1s ease 4s forwards;
}

@keyframes fadeOut {
  to { opacity: 0; visibility: hidden; }
}

.loader-ring {
  width: 120px; height: 120px;
  border: 6px solid rgba(255,0,0,0.2);
  border-top-color: #ff0033;
  border-radius: 50%;
  animation: spin 2s linear infinite, glow 2s ease-in-out infinite alternate;
}

@keyframes spin { to { transform: rotate(360deg); } }
@keyframes glow {
  from { box-shadow: 0 0 10px #ff0033, 0 0 30px #ff3300; }
  to { box-shadow: 0 0 30px #ff0033, 0 0 60px #ff3300; }
}

.loader-text {
  margin-top: 25px;
  font-size: 1.8em;
  background: linear-gradient(90deg, #ff3300, #ff0033, #990000);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0%,100% { opacity: 0.6; }
  50% { opacity: 1; }
}

#menu {
  display: none;
  align-items: center;
  justify-content: center;
  height: 100vh;
  flex-direction: column;
  animation: fadeIn 2s ease forwards;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.hologram {
  position: relative;
  width: 280px; height: 280px;
  border-radius: 15px;
  overflow: hidden;
  box-shadow: 0 0 40px #ff0000;
  animation: borderRotate 6s linear infinite;
}

@keyframes borderRotate {
  0% { box-shadow: 0 0 20px #ff0000, inset 0 0 10px #ff0000; }
  25% { box-shadow: 0 0 25px #ff5500, inset 0 0 15px #ff3300; }
  50% { box-shadow: 0 0 30px #ff6600, inset 0 0 20px #ff3300; }
  75% { box-shadow: 0 0 25px #ff3300, inset 0 0 15px #ff1100; }
  100% { box-shadow: 0 0 20px #ff0000, inset 0 0 10px #ff0000; }
}

.hologram img {
  width: 100%; height: 100%;
  object-fit: cover;
  opacity: 0.9;
}

.energy-line {
  position: absolute;
  width: 100%; height: 2px;
  top: 0; left: 0;
  background: linear-gradient(90deg, transparent, #ff3300, transparent);
  animation: energyRun 2s linear infinite;
}
@keyframes energyRun {
  0% { top: 0; opacity: 0; }
  50% { top: 100%; opacity: 1; }
  100% { top: 0; opacity: 0; }
}

.menu-panel {
  margin-top: 50px;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  background: rgba(255,0,0,0.08);
  border: 1px solid rgba(255,0,0,0.3);
  padding: 20px;
  border-radius: 12px;
  backdrop-filter: blur(10px);
  box-shadow: 0 0 20px rgba(255,0,0,0.3);
}

button {
  background: rgba(50, 0, 0, 0.6);
  border: 1px solid #ff0000;
  color: #fff;
  padding: 12px 20px;
  border-radius: 10px;
  font-size: 14px;
  cursor: pointer;
  position: relative;
  transition: 0.3s;
  overflow: hidden;
  font-weight: bold;
}

button:hover {
  background: rgba(255, 0, 0, 0.7);
  box-shadow: 0 0 15px #ff0033;
}

button.active {
  background: linear-gradient(90deg, #ff0000, #660000);
  box-shadow: 0 0 25px #ff3300, 0 0 50px #ff0000;
}

button.active::after {
  content: "";
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: rgba(255,255,255,0.3);
  animation: sweep 1s linear infinite;
}
@keyframes sweep {
  0% { left: -100%; }
  100% { left: 100%; }
}

.toast {
  position: fixed;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(0,0,0,0.85);
  border: 1px solid #ff0000;
  color: #ff6666;
  padding: 12px 25px;
  border-radius: 8px;
  opacity: 0;
  transition: opacity 0.5s;
  z-index: 10001;
  text-shadow: 0 0 10px #ff0000;
}
.toast.show { opacity: 1; }
</style>
</head>
<body>
<div id="loading">
  <div class="loader-ring"></div>
  <div class="loader-text">Initializing Kích Hoạt Hệ Thống Panel</div>
</div>

<div id="menu">
  <div class="hologram">
    <img src="IMG_0076.JPG" alt="Thắng Ôm Em">
    <div class="energy-line"></div>
  </div>
  <div class="menu-panel">
    <button onclick="toggleFeature(this, 'DpI Setup')">DpI Setup</button>
    <button onclick="toggleFeature(this, 'AutoHead')">Auto Head</button>
    <button onclick="toggleFeature(this, 'LockHead')">Lock Head</button>
    <button onclick="toggleFeature(this, 'Dplconfig')">Dplconfig</button>
    <button onclick=“toggleFeature(this, 'FPS Setting')">FPS Setting</button>
    <button onclick="toggleFeature(this, 'HDR Sivion')">HDR Sivion</button>
  </div>
</div>

<div id="toast" class="toast"></div>

<script>
window.addEventListener("load", () => {
  setTimeout(() => {
    document.getElementById("menu").style.display = "flex";
  }, 4000);
});

function toggleFeature(btn, feature) {
  btn.classList.toggle("active");
  showToast(feature + (btn.classList.contains("active") ? " Kích hoạt" : " Tắt"));
}

function showToast(msg) {
  const toast = document.getElementById("toast");
  toast.textContent = msg;
  toast.classList.add("show");
  setTimeout(() => toast.classList.remove("show"), 2000);
}
</script>
</body>
</html>
